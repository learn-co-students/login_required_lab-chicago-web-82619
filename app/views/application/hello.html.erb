<% if session[:name] != nil %>

    <h1>Hi, <%= session[:name] %></h1>

    <%= link_to "Secret Time", "/secret" %>

    <%= form_tag ({controller: 'sessions', action: 'destroy', method: 'post'}) do %>
        <input type=submit value='logout'>
    <% end %>

<% else %>

    <%= button_to "Login", login_path, method: "get"%>

<% end %>